name: CinePulse Build & Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4


      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app

      - name: Build Project
        run: |
          xcodebuild clean build \
            -workspace Cine-Pulse.xcworkspace \
            -scheme Cine-Pulse \
            -sdk iphonesimulator \
            -destination 'platform=iOS Simulator,name=iPhone 15'

      - name: Run Tests
        run: |
          xcodebuild test \
            -workspace Cine-Pulse.xcworkspace \
            -scheme Cine-Pulse \
            -sdk iphonesimulator \
            -destination 'platform=iOS Simulator,name=iPhone 15'.0'


